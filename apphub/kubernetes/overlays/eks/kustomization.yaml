apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: t4hub-prod

resources:
  - ../../base
  - namespace.yaml
  - storageclass.yaml
  - pvcs.yaml
  - secrets.yaml
  - ingress.yaml

patches:
  - path: patch-apphub-pod.yaml
  - path: patch-postgresql.yaml
